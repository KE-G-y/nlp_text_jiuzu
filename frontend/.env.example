# 后端分类接口地址。默认走 Vite 代理到当前后端服务：http://localhost:8003/predict。
VITE_CLASSIFY_API_URL=/api/predict

# 可选：强制 mock 或强制真实接口。true 使用 mock，false 使用接口。
VITE_USE_MOCK=

# 可选：接口请求超时时间，单位毫秒。
VITE_CLASSIFY_TIMEOUT_MS=12000
